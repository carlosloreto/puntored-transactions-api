spring:
  profiles:
    active: test
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

supabase:
  jwt-secret: test-secret-key-for-testing-purposes-only-must-be-at-least-256-bits-long-for-hs256-algorithm
  jwt-issuer: https://test.supabase.co/auth/v1

logging:
  level:
    com.puntored.transactions_api: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
